//acid platform information
$.lucy = {
	//lib name
	name: 'Lucy',
	//lib version
	version: 1,
	//platform type
	platform: 'stable'
};
//log out the ACID version
console.log(`Lucy v${$.lucy.version}`);
